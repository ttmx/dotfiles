#!/bin/sh
case "$(printf "a selected area\\ncurrent window\\nfull screen\\na selected area (copy)\\ncurrent window (copy)\\nfull screen (copy)" | rofi -dmenu -p "Screenshot which area?")" in
	"a selected area") maim -s $SCREENSHOTS/pic-selected-"$(date '+%y%m%d-%H%M-%S').png" ;;
	"current window") maim -u -i "$(xdotool getactivewindow)" $SCREENSHOTS/pic-window-"$(date '+%y%m%d-%H%M-%S').png" ;;
	"full screen") sleep 0.1; maim $SCREENSHOTS/pic-full-"$(date '+%y%m%d-%H%M-%S').png" ;;
	"a selected area (copy)") sleep 0.1; freezemaim;;
	"current window (copy)") maim -i "$(xdotool getactivewindow)" | xclip -selection clipboard -t image/png ;;
	"full screen (copy)") sleep 0.1; maim | xclip -selection clipboard -t image/png ;;
esac
